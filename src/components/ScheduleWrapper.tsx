import styled from "styled-components";

const ScheduleWrapper = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 80px;
  border-top: 2px solid #7775;
  padding-top: 40px;
`;

export default ScheduleWrapper;